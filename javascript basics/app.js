// Template Literals - ES6+
// Backtick characters `` - above tab
// Interpolation ${} - insert expression(value)

const name = "igor";
const age = 28;
const setence = "Hey its me " + name + ", and I have " + age + " years old.";
console.log(setence);

const value = `Hey it's me ${name}, and I have ${age} years old`;
console.log(value);
